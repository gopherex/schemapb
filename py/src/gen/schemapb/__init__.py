# Generated by the protocol buffer compiler.  DO NOT EDIT!
# sources: schemapb/errors.proto, schemapb/runtime.proto, schemapb/schema.proto, schemapb/value.proto
# plugin: python-betterproto2
# This file has been @generated

__all__ = (
    "Baked",
    "ErrorCode",
    "Filled",
    "ListValue",
    "NullValue",
    "Schema",
    "SchemaField",
    "SchemaFieldBool",
    "SchemaFieldBytes",
    "SchemaFieldChoice",
    "SchemaFieldChoiceOption",
    "SchemaFieldComputed",
    "SchemaFieldDouble",
    "SchemaFieldDuration",
    "SchemaFieldFloat",
    "SchemaFieldInt32",
    "SchemaFieldInt64",
    "SchemaFieldJson",
    "SchemaFieldList",
    "SchemaFieldMap",
    "SchemaFieldObject",
    "SchemaFieldOneOf",
    "SchemaFieldRef",
    "SchemaFieldResultType",
    "SchemaFieldRule",
    "SchemaFieldSeverity",
    "SchemaFieldString",
    "SchemaFieldTimestamp",
    "SchemaFieldUInt32",
    "SchemaFieldUInt64",
    "SchemaIdentity",
    "SchemaRef",
    "StructValue",
    "ValidationError",
    "ValidationResult",
    "Value",
)

import builtins
import datetime
from dataclasses import dataclass

import betterproto2

from ..message_pool import default_message_pool

_COMPILER_VERSION = "0.10.1"
betterproto2.check_compiler_version(_COMPILER_VERSION)


class ErrorCode(betterproto2.Enum):
    """

    ErrorCode is the stable, machine-readable reason a validation failed. The
    code (plus path and the typed fields on ValidationError) IS the
    cross-implementation contract; human-readable text is rendered from a
    shared, spec-owned template set and is not part of the contract.
    """

    UNSPECIFIED = 0

    TYPE_MISMATCH = 1
    """
    Value/type shape. 
    """

    REQUIRED_MISSING = 2

    NOT_NULLABLE = 3

    UNKNOWN_FIELD = 4

    IMMUTABLE_MODIFIED = 5

    CONST_MISMATCH = 10
    """
    Scalar constraints. 
    """

    GT_VIOLATED = 11

    GTE_VIOLATED = 12

    LT_VIOLATED = 13

    LTE_VIOLATED = 14

    NOT_IN_ALLOWED_SET = 15

    IN_FORBIDDEN_SET = 16

    MULTIPLE_OF_VIOLATED = 17

    LEN_MISMATCH = 20
    """
    String constraints. 
    """

    MIN_LEN_VIOLATED = 21

    MAX_LEN_VIOLATED = 22

    PATTERN_MISMATCH = 23

    FORMAT_MISMATCH = 24

    PREFIX_MISMATCH = 25
    """
    String/bytes affix constraints. 
    """

    SUFFIX_MISMATCH = 26

    UNSUPPORTED_FORMAT = 27
    """
    The schema names a format this implementation does not support. Failing
    loudly is mandatory: a format may never be silently skipped. 
    """

    CHOICE_NOT_ALLOWED = 30
    """
    Choice constraints. 
    """

    MIN_ITEMS_VIOLATED = 40
    """
    List constraints. 
    """

    MAX_ITEMS_VIOLATED = 41

    NOT_UNIQUE = 42

    LIST_COUNT_MISMATCH = 43

    MIN_ENTRIES_VIOLATED = 50
    """
    Map constraints. 
    """

    MAX_ENTRIES_VIOLATED = 51

    MIN_PROPERTIES_VIOLATED = 60
    """
    Form-level constraints. 
    """

    MAX_PROPERTIES_VIOLATED = 61

    DISCRIMINATOR_MISSING = 70
    """
    Composition. 
    """

    UNKNOWN_VARIANT = 71

    UNKNOWN_REF = 72

    RULE_VIOLATED = 80
    """
    CEL layer. 
    """

    EXPR_ERROR = 81

    INVALID_SCHEMA = 90
    """
    Schema descriptor itself is malformed (bad identity, no kind chosen,
    computed-field cycle, invalid CEL/Mustache/pattern source, ...). 
    """

    @classmethod
    def betterproto_value_to_renamed_proto_names(cls) -> dict[int, str]:
        return {
            0: "ERROR_CODE_UNSPECIFIED",
            1: "ERROR_CODE_TYPE_MISMATCH",
            2: "ERROR_CODE_REQUIRED_MISSING",
            3: "ERROR_CODE_NOT_NULLABLE",
            4: "ERROR_CODE_UNKNOWN_FIELD",
            5: "ERROR_CODE_IMMUTABLE_MODIFIED",
            10: "ERROR_CODE_CONST_MISMATCH",
            11: "ERROR_CODE_GT_VIOLATED",
            12: "ERROR_CODE_GTE_VIOLATED",
            13: "ERROR_CODE_LT_VIOLATED",
            14: "ERROR_CODE_LTE_VIOLATED",
            15: "ERROR_CODE_NOT_IN_ALLOWED_SET",
            16: "ERROR_CODE_IN_FORBIDDEN_SET",
            17: "ERROR_CODE_MULTIPLE_OF_VIOLATED",
            20: "ERROR_CODE_LEN_MISMATCH",
            21: "ERROR_CODE_MIN_LEN_VIOLATED",
            22: "ERROR_CODE_MAX_LEN_VIOLATED",
            23: "ERROR_CODE_PATTERN_MISMATCH",
            24: "ERROR_CODE_FORMAT_MISMATCH",
            25: "ERROR_CODE_PREFIX_MISMATCH",
            26: "ERROR_CODE_SUFFIX_MISMATCH",
            27: "ERROR_CODE_UNSUPPORTED_FORMAT",
            30: "ERROR_CODE_CHOICE_NOT_ALLOWED",
            40: "ERROR_CODE_MIN_ITEMS_VIOLATED",
            41: "ERROR_CODE_MAX_ITEMS_VIOLATED",
            42: "ERROR_CODE_NOT_UNIQUE",
            43: "ERROR_CODE_LIST_COUNT_MISMATCH",
            50: "ERROR_CODE_MIN_ENTRIES_VIOLATED",
            51: "ERROR_CODE_MAX_ENTRIES_VIOLATED",
            60: "ERROR_CODE_MIN_PROPERTIES_VIOLATED",
            61: "ERROR_CODE_MAX_PROPERTIES_VIOLATED",
            70: "ERROR_CODE_DISCRIMINATOR_MISSING",
            71: "ERROR_CODE_UNKNOWN_VARIANT",
            72: "ERROR_CODE_UNKNOWN_REF",
            80: "ERROR_CODE_RULE_VIOLATED",
            81: "ERROR_CODE_EXPR_ERROR",
            90: "ERROR_CODE_INVALID_SCHEMA",
        }

    @classmethod
    def betterproto_renamed_proto_names_to_value(cls) -> dict[str, int]:
        return {
            "ERROR_CODE_UNSPECIFIED": 0,
            "ERROR_CODE_TYPE_MISMATCH": 1,
            "ERROR_CODE_REQUIRED_MISSING": 2,
            "ERROR_CODE_NOT_NULLABLE": 3,
            "ERROR_CODE_UNKNOWN_FIELD": 4,
            "ERROR_CODE_IMMUTABLE_MODIFIED": 5,
            "ERROR_CODE_CONST_MISMATCH": 10,
            "ERROR_CODE_GT_VIOLATED": 11,
            "ERROR_CODE_GTE_VIOLATED": 12,
            "ERROR_CODE_LT_VIOLATED": 13,
            "ERROR_CODE_LTE_VIOLATED": 14,
            "ERROR_CODE_NOT_IN_ALLOWED_SET": 15,
            "ERROR_CODE_IN_FORBIDDEN_SET": 16,
            "ERROR_CODE_MULTIPLE_OF_VIOLATED": 17,
            "ERROR_CODE_LEN_MISMATCH": 20,
            "ERROR_CODE_MIN_LEN_VIOLATED": 21,
            "ERROR_CODE_MAX_LEN_VIOLATED": 22,
            "ERROR_CODE_PATTERN_MISMATCH": 23,
            "ERROR_CODE_FORMAT_MISMATCH": 24,
            "ERROR_CODE_PREFIX_MISMATCH": 25,
            "ERROR_CODE_SUFFIX_MISMATCH": 26,
            "ERROR_CODE_UNSUPPORTED_FORMAT": 27,
            "ERROR_CODE_CHOICE_NOT_ALLOWED": 30,
            "ERROR_CODE_MIN_ITEMS_VIOLATED": 40,
            "ERROR_CODE_MAX_ITEMS_VIOLATED": 41,
            "ERROR_CODE_NOT_UNIQUE": 42,
            "ERROR_CODE_LIST_COUNT_MISMATCH": 43,
            "ERROR_CODE_MIN_ENTRIES_VIOLATED": 50,
            "ERROR_CODE_MAX_ENTRIES_VIOLATED": 51,
            "ERROR_CODE_MIN_PROPERTIES_VIOLATED": 60,
            "ERROR_CODE_MAX_PROPERTIES_VIOLATED": 61,
            "ERROR_CODE_DISCRIMINATOR_MISSING": 70,
            "ERROR_CODE_UNKNOWN_VARIANT": 71,
            "ERROR_CODE_UNKNOWN_REF": 72,
            "ERROR_CODE_RULE_VIOLATED": 80,
            "ERROR_CODE_EXPR_ERROR": 81,
            "ERROR_CODE_INVALID_SCHEMA": 90,
        }


class NullValue(betterproto2.Enum):
    """
    NullValue is the singleton null.
    """

    NULL_VALUE = 0


class SchemaFieldResultType(betterproto2.Enum):
    """
    ResultType is the value type a Computed expression yields.
    """

    UNSPECIFIED = 0
    """
    Unset: infer from the expression. 
    """

    DOUBLE = 1

    INT64 = 2

    UINT64 = 3

    BOOL = 4

    STRING = 5

    DURATION = 6

    TIMESTAMP = 7

    BYTES = 8

    JSON = 9
    """
    Arbitrary Value: the expression result is stored as-is (dyn). 
    """

    @classmethod
    def betterproto_value_to_renamed_proto_names(cls) -> dict[int, str]:
        return {
            0: "RESULT_TYPE_UNSPECIFIED",
            1: "RESULT_TYPE_DOUBLE",
            2: "RESULT_TYPE_INT64",
            3: "RESULT_TYPE_UINT64",
            4: "RESULT_TYPE_BOOL",
            5: "RESULT_TYPE_STRING",
            6: "RESULT_TYPE_DURATION",
            7: "RESULT_TYPE_TIMESTAMP",
            8: "RESULT_TYPE_BYTES",
            9: "RESULT_TYPE_JSON",
        }

    @classmethod
    def betterproto_renamed_proto_names_to_value(cls) -> dict[str, int]:
        return {
            "RESULT_TYPE_UNSPECIFIED": 0,
            "RESULT_TYPE_DOUBLE": 1,
            "RESULT_TYPE_INT64": 2,
            "RESULT_TYPE_UINT64": 3,
            "RESULT_TYPE_BOOL": 4,
            "RESULT_TYPE_STRING": 5,
            "RESULT_TYPE_DURATION": 6,
            "RESULT_TYPE_TIMESTAMP": 7,
            "RESULT_TYPE_BYTES": 8,
            "RESULT_TYPE_JSON": 9,
        }


class SchemaFieldSeverity(betterproto2.Enum):
    """
    Severity of a failed rule.
    """

    UNSPECIFIED = 0
    """
    Unset: treated as ERROR. 
    """

    ERROR = 1
    """
    Blocks submit. 
    """

    WARNING = 2
    """
    Surfaced to the user but does not block submit. 
    """

    @classmethod
    def betterproto_value_to_renamed_proto_names(cls) -> dict[int, str]:
        return {
            0: "SEVERITY_UNSPECIFIED",
            1: "SEVERITY_ERROR",
            2: "SEVERITY_WARNING",
        }

    @classmethod
    def betterproto_renamed_proto_names_to_value(cls) -> dict[str, int]:
        return {
            "SEVERITY_UNSPECIFIED": 0,
            "SEVERITY_ERROR": 1,
            "SEVERITY_WARNING": 2,
        }


@dataclass(eq=False, repr=False)
class Baked(betterproto2.Message):
    """

    Baked is a sealed result: the final values together with the schema they
    were validated and resolved against. It is a self-contained, immutable
    snapshot — "these exact values for this exact schema, not to be changed".
    Unlike Filled (which only references a schema and is mutable), Baked embeds
    the full schema and the frozen values, so it is portable and verifiable on
    its own. Whether a Baked still matches a given schema is checked by deep
    message equality of the embedded schema.
    """

    schema: "Schema | None" = betterproto2.field(
        1, betterproto2.TYPE_MESSAGE, optional=True
    )
    """
    The schema these values were baked against (embedded, self-contained). 
    """

    values: "StructValue | None" = betterproto2.field(
        2, betterproto2.TYPE_MESSAGE, optional=True
    )
    """
    The final, resolved values — frozen. 
    """


default_message_pool.register_message("schemapb", "Baked", Baked)


@dataclass(eq=False, repr=False)
class Filled(betterproto2.Message):
    """

    Filled is a runtime form: a reference to the schema plus the values entered
    for it. The schema says what is allowed/derived; the values are what to
    check/resolve. It is mutable input (the thing being edited/validated).
    """

    schema: "SchemaRef | None" = betterproto2.field(
        1, betterproto2.TYPE_MESSAGE, optional=True
    )
    """
    Schema this form is filled against (by-id or inline). 
    """

    values: "StructValue | None" = betterproto2.field(
        2, betterproto2.TYPE_MESSAGE, optional=True
    )
    """
    Form values the expressions read and the validator checks. 
    """


default_message_pool.register_message("schemapb", "Filled", Filled)


@dataclass(eq=False, repr=False)
class ListValue(betterproto2.Message):
    """
    ListValue is an ordered sequence of values.
    """

    items: "list[Value]" = betterproto2.field(
        1, betterproto2.TYPE_MESSAGE, repeated=True
    )


default_message_pool.register_message("schemapb", "ListValue", ListValue)


@dataclass(eq=False, repr=False)
class Schema(betterproto2.Message):
    """

    Schema is a runtime, dynamic form/config descriptor. A producer builds a
    Schema and ships it (as a protobuf message) to any consumer — Go, TypeScript,
    Python or Rust — which validates values, computes derived fields and renders
    text output natively. Cross-implementation agreement is guaranteed by the
    schemapb spec and its conformance suite, not by sharing a runtime.

    Validation has two layers:
      - per-kind structured rules (gt/gte/lt/lte/in/pattern/...) for the common,
        single-field cases. Ergonomic to author and introspectable by renderers
        (e.g. slider bounds).
      - Field.rules / Schema.rules: CEL expressions (https://cel.dev) for
        cross-field and conditional validation.

    Expression language: full CEL spec plus the `strings` extension. Expressions
    must be pure and deterministic (no now()/rand, no implicit timezones) so all
    implementations agree. Variables bound by the engine:
      `this` — the current field's value (field-level rules/normalize only)
      `root` — the whole form as a map (inputs + already-computed values)

    Render templates are Mustache (https://mustache.github.io); see
    Schema.templates.
    """

    id: "SchemaIdentity | None" = betterproto2.field(
        1, betterproto2.TYPE_MESSAGE, optional=True
    )
    """
    Stable identity of this schema. Required: the validator rejects a schema
    whose id is unset or whose name is empty. 
    """

    description: "str | None" = betterproto2.field(
        2, betterproto2.TYPE_STRING, optional=True
    )
    """
    Human description of the schema. 
    """

    fields: "list[SchemaField]" = betterproto2.field(
        3, betterproto2.TYPE_MESSAGE, repeated=True
    )
    """
    The fields that make up the form. 
    """

    rules: "list[SchemaFieldRule]" = betterproto2.field(
        4, betterproto2.TYPE_MESSAGE, repeated=True
    )
    """
    Form-wide invariant rules (`root` only, no `this`). 
    """

    strict: "bool" = betterproto2.field(5, betterproto2.TYPE_BOOL)
    """
    
    If true, any key in the values map that is not a declared field name is
    an error (ERROR_CODE_UNKNOWN_FIELD).
    """

    min_properties: "int | None" = betterproto2.field(
        6, betterproto2.TYPE_UINT64, optional=True
    )
    """
    Minimum number of properties that must be present in the values map. 
    """

    max_properties: "int | None" = betterproto2.field(
        7, betterproto2.TYPE_UINT64, optional=True
    )
    """
    Maximum number of properties allowed in the values map. 
    """

    coerce: "bool" = betterproto2.field(8, betterproto2.TYPE_BOOL)
    """
    
    If true, scalar string inputs are coerced to the field's kind before
    validation (numeric kinds parse the string as a number; bool parses
    "true"/"false"; enum parses an integer string). Only applied when the
    current value is a string but the field's kind expects a different type
    and parsing succeeds; unparseable strings are left as-is so the type
    error is reported normally.
    """

    defs: "dict[str, Schema]" = betterproto2.field(
        9,
        betterproto2.TYPE_MAP,
        map_meta=betterproto2.map_meta(
            betterproto2.TYPE_STRING, betterproto2.TYPE_MESSAGE
        ),
    )
    """
    
    Named reusable sub-schemas ($defs). Fields of kind Ref resolve their
    value against the named def found here. Meaningful only on the root
    schema; defs inside nested schemas are ignored by the engine.
    """

    templates: "dict[str, str]" = betterproto2.field(
        10,
        betterproto2.TYPE_MAP,
        map_meta=betterproto2.map_meta(
            betterproto2.TYPE_STRING, betterproto2.TYPE_STRING
        ),
    )
    """
    
    Named render templates (Mustache syntax, https://mustache.github.io) for
    serialising resolved values into a target text format, e.g. "conf" -> a
    postgresql.conf template. Portable data: every implementation renders
    them identically (per the Mustache spec and the schemapb render-context
    spec). The template executes against a context exposing the ordered
    fields, fields grouped by `group`, and the raw values; formatting
    helpers (on/off, yes/no, quoted, upper/lower) are precomputed context
    fields, not template functions. Complex logic belongs in Computed
    fields, not in templates. The validation/compute engine ignores these.
    """


default_message_pool.register_message("schemapb", "Schema", Schema)


@dataclass(eq=False, repr=False)
class SchemaField(betterproto2.Message):
    """

    Field is one field of the form.

    Per-field evaluation order (each stage reads the form as resolved by the
    previous one):
      1. when         — gate: if false the field is INACTIVE and every stage
                        below is skipped; the field is treated as ABSENT (its
                        value, if any, is preserved but ignored). For a
                        container kind (Object/OneOf/List/Map/Ref) the WHOLE
                        subtree is gated.
      2. normalize    — map the field's own value.
      3. Computed     — derive the value from `root`.
      4. options_expr / count_expr — dynamic Choice options / List length.
      5. rules + kind constraints — required/nullable, gt/lt/in/pattern, ...

    Renderer contract: an inactive field (when=false) MUST NOT be rendered.

    Oneofs:
        - kind: The field kind; exactly one must be set.
    """

    name: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    Field name; the key under which the value lives in the form. 
    """

    description: "str | None" = betterproto2.field(
        2, betterproto2.TYPE_STRING, optional=True
    )
    """
    Human description of the field. 
    """

    nullable: "bool" = betterproto2.field(3, betterproto2.TYPE_BOOL)
    """
    If true, the value may be null/empty. 
    """

    required: "bool" = betterproto2.field(4, betterproto2.TYPE_BOOL)
    """
    If true, the value must be present. 
    """

    rules: "list[SchemaFieldRule]" = betterproto2.field(
        5, betterproto2.TYPE_MESSAGE, repeated=True
    )
    """
    Cross-field CEL validation rules (`this` is bound to this field). 
    """

    float: "SchemaFieldFloat | None" = betterproto2.field(
        6, betterproto2.TYPE_MESSAGE, optional=True, group="kind"
    )
    """
    Float field. 
    """

    double: "SchemaFieldDouble | None" = betterproto2.field(
        7, betterproto2.TYPE_MESSAGE, optional=True, group="kind"
    )
    """
    Double field. 
    """

    int32: "SchemaFieldInt32 | None" = betterproto2.field(
        8, betterproto2.TYPE_MESSAGE, optional=True, group="kind"
    )
    """
    Int32 field. 
    """

    int64: "SchemaFieldInt64 | None" = betterproto2.field(
        9, betterproto2.TYPE_MESSAGE, optional=True, group="kind"
    )
    """
    Int64 field. 
    """

    uint32: "SchemaFieldUInt32 | None" = betterproto2.field(
        10, betterproto2.TYPE_MESSAGE, optional=True, group="kind"
    )
    """
    UInt32 field. 
    """

    uint64: "SchemaFieldUInt64 | None" = betterproto2.field(
        11, betterproto2.TYPE_MESSAGE, optional=True, group="kind"
    )
    """
    UInt64 field. 
    """

    bool: "SchemaFieldBool | None" = betterproto2.field(
        12, betterproto2.TYPE_MESSAGE, optional=True, group="kind"
    )
    """
    Bool field. 
    """

    string: "SchemaFieldString | None" = betterproto2.field(
        13, betterproto2.TYPE_MESSAGE, optional=True, group="kind"
    )
    """
    String field. 
    """

    choice: "SchemaFieldChoice | None" = betterproto2.field(
        14, betterproto2.TYPE_MESSAGE, optional=True, group="kind"
    )
    """
    Choice (closed value set) field. 
    """

    duration: "SchemaFieldDuration | None" = betterproto2.field(
        15, betterproto2.TYPE_MESSAGE, optional=True, group="kind"
    )
    """
    Duration field. 
    """

    timestamp: "SchemaFieldTimestamp | None" = betterproto2.field(
        16, betterproto2.TYPE_MESSAGE, optional=True, group="kind"
    )
    """
    Timestamp field. 
    """

    list: "SchemaFieldList | None" = betterproto2.field(
        17, betterproto2.TYPE_MESSAGE, optional=True, group="kind"
    )
    """
    List field. 
    """

    object: "SchemaFieldObject | None" = betterproto2.field(
        18, betterproto2.TYPE_MESSAGE, optional=True, group="kind"
    )
    """
    Nested object field. 
    """

    computed: "SchemaFieldComputed | None" = betterproto2.field(
        19, betterproto2.TYPE_MESSAGE, optional=True, group="kind"
    )
    """
    Computed (derived) field. 
    """

    one_of: "SchemaFieldOneOf | None" = betterproto2.field(
        28, betterproto2.TYPE_MESSAGE, optional=True, group="kind"
    )
    """
    Discriminated union field. 
    """

    ref: "SchemaFieldRef | None" = betterproto2.field(
        29, betterproto2.TYPE_MESSAGE, optional=True, group="kind"
    )
    """
    Reference to a named definition in the root schema's defs. 
    """

    map: "SchemaFieldMap | None" = betterproto2.field(
        31, betterproto2.TYPE_MESSAGE, optional=True, group="kind"
    )
    """
    Free-key, typed-value map field. 
    """

    bytes: "SchemaFieldBytes | None" = betterproto2.field(
        32, betterproto2.TYPE_MESSAGE, optional=True, group="kind"
    )
    """
    Bytes field. 
    """

    json: "SchemaFieldJson | None" = betterproto2.field(
        33, betterproto2.TYPE_MESSAGE, optional=True, group="kind"
    )
    """
    Free-form JSON value field. 
    """

    immutable: "builtins.bool" = betterproto2.field(20, betterproto2.TYPE_BOOL)
    """
    
    If true, the value is system-fixed: it equals `default` and cannot be
    changed. The validator forces it to `default` and rejects any
    different submitted value; renderers show it disabled. A "system
    value" is therefore immutable + default.
    """

    group: "str | None" = betterproto2.field(
        21, betterproto2.TYPE_STRING, optional=True
    )
    """
    
    Informative section label for grouping fields (e.g. "WAL"). Purely
    informative — the validation/compute engine ignores it.
    """

    unit: "str | None" = betterproto2.field(22, betterproto2.TYPE_STRING, optional=True)
    """
    
    Informative unit of the value, e.g. "MB" or "ms". Purely informative —
    the engine ignores it; it is for renderers/serializers.
    """

    title: "str | None" = betterproto2.field(
        23, betterproto2.TYPE_STRING, optional=True
    )
    """
    Informative human title for the field. Engine ignores it. 
    """

    deprecated: "builtins.bool" = betterproto2.field(24, betterproto2.TYPE_BOOL)
    """
    If true, this field is deprecated; renderers may warn. Informative. 
    """

    examples: "list[Value]" = betterproto2.field(
        25, betterproto2.TYPE_MESSAGE, repeated=True
    )
    """
    Example values for documentation/rendering. Purely informative. 
    """

    secret: "builtins.bool" = betterproto2.field(26, betterproto2.TYPE_BOOL)
    """
    If true, the value is sensitive (e.g. a password). Informative. 
    """

    normalize: "str | None" = betterproto2.field(
        27, betterproto2.TYPE_STRING, optional=True
    )
    """
    
    Normalize expression: a CEL expression mapping the field's own value
    before validation. `this` = current value, `root` = whole form.
    Returns the new value. Applied after defaults/coercion and before
    Computed evaluation and validation.
    """

    when: "str | None" = betterproto2.field(30, betterproto2.TYPE_STRING, optional=True)
    """
    
    Conditional gate: a CEL boolean expression over `root` (the whole form
    as a map). When it evaluates to false the field is INACTIVE — the
    validator skips it ENTIRELY (no required/nullable, no rules, no kind
    constraints, no Computed/normalize) and treats it as ABSENT regardless
    of any value present in `values`. For a container kind the whole
    subtree is gated. Inactive fields do not count toward
    min/max_properties and their value key never raises a strict
    ERROR_CODE_UNKNOWN_FIELD. Their value is NOT deleted, so it reappears
    if the field becomes active again. Renderers MUST hide an inactive
    field. `this` is NOT bound (a field's own value must not gate its
    existence). Empty/absent => always active. A non-bool result is a
    runtime error.
    """


default_message_pool.register_message("schemapb", "Schema.Field", SchemaField)


@dataclass(eq=False, repr=False)
class SchemaFieldBool(betterproto2.Message):
    """
    Bool field kind.
    """

    default: "bool | None" = betterproto2.field(
        1, betterproto2.TYPE_BOOL, optional=True
    )
    """
    Default value used when the field is unset. 
    """

    const: "bool | None" = betterproto2.field(2, betterproto2.TYPE_BOOL, optional=True)
    """
    Value must equal exactly this. 
    """


default_message_pool.register_message("schemapb", "Schema.Field.Bool", SchemaFieldBool)


@dataclass(eq=False, repr=False)
class SchemaFieldBytes(betterproto2.Message):
    """
    Bytes field kind and its single-field constraints. Values travel as
    protobuf bytes (base64 in protoJSON) and are CEL-native `bytes`.
    """

    default: "bytes | None" = betterproto2.field(
        1, betterproto2.TYPE_BYTES, optional=True
    )
    """
    Default value used when the field is unset. 
    """

    const: "bytes | None" = betterproto2.field(
        2, betterproto2.TYPE_BYTES, optional=True
    )
    """
    Value must equal exactly this. 
    """

    len: "int | None" = betterproto2.field(3, betterproto2.TYPE_UINT64, optional=True)
    """
    Exact byte length required. 
    """

    min_len: "int | None" = betterproto2.field(
        4, betterproto2.TYPE_UINT64, optional=True
    )
    """
    Minimum byte length. 
    """

    max_len: "int | None" = betterproto2.field(
        5, betterproto2.TYPE_UINT64, optional=True
    )
    """
    Maximum byte length. 
    """

    prefix: "bytes | None" = betterproto2.field(
        6, betterproto2.TYPE_BYTES, optional=True
    )
    """
    Value must start with this prefix. 
    """

    suffix: "bytes | None" = betterproto2.field(
        7, betterproto2.TYPE_BYTES, optional=True
    )
    """
    Value must end with this suffix. 
    """

    in_: "list[bytes]" = betterproto2.field(8, betterproto2.TYPE_BYTES, repeated=True)
    """
    Value must be one of these. 
    """

    not_in: "list[bytes]" = betterproto2.field(
        9, betterproto2.TYPE_BYTES, repeated=True
    )
    """
    Value must not be any of these. 
    """


default_message_pool.register_message(
    "schemapb", "Schema.Field.Bytes", SchemaFieldBytes
)


@dataclass(eq=False, repr=False)
class SchemaFieldChoice(betterproto2.Message):
    """

    Choice field kind: a closed (or advisory) set of allowed values with
    optional human labels — the "enum" of schemapb, generalised. Options
    carry typed Values, so string-valued domains ("ssd"/"hdd",
    "minimal"/"replica"/"logical") are first-class: no artificial integer
    codes. Renderers draw a select/dropdown from options (label falls
    back to the value's display form).
    """

    options: "list[SchemaFieldChoiceOption]" = betterproto2.field(
        1, betterproto2.TYPE_MESSAGE, repeated=True
    )
    """
    The static allowed set (and the renderer's option list). 
    """

    default: "Value | None" = betterproto2.field(
        2, betterproto2.TYPE_MESSAGE, optional=True
    )
    """
    Default value used when the field is unset. 
    """

    open: "bool" = betterproto2.field(3, betterproto2.TYPE_BOOL)
    """
    
    If true the set is advisory: values outside options validate fine
    (options become suggestions). If false (default) a value not in the
    allowed set is ERROR_CODE_CHOICE_NOT_ALLOWED.
    """

    options_expr: "str | None" = betterproto2.field(
        4, betterproto2.TYPE_STRING, optional=True
    )
    """
    
    CEL expression over `root` returning the list of allowed values.
    When set it REPLACES the static options for validation and supplies
    the option list to renderers. Empty/absent => use options. A
    non-list result is a runtime error. Use cases: db versions by kind,
    zones by region.
    """


default_message_pool.register_message(
    "schemapb", "Schema.Field.Choice", SchemaFieldChoice
)


@dataclass(eq=False, repr=False)
class SchemaFieldChoiceOption(betterproto2.Message):
    """
    One selectable option.
    """

    value: "Value | None" = betterproto2.field(
        1, betterproto2.TYPE_MESSAGE, optional=True
    )
    """
    The value this option stands for (typed). 
    """

    label: "str" = betterproto2.field(2, betterproto2.TYPE_STRING)
    """
    Human label for renderers; empty => display the value itself. 
    """

    description: "str" = betterproto2.field(3, betterproto2.TYPE_STRING)
    """
    Human description of the option. 
    """

    deprecated: "bool" = betterproto2.field(4, betterproto2.TYPE_BOOL)
    """
    Renderers may hide or warn on deprecated options. 
    """


default_message_pool.register_message(
    "schemapb", "Schema.Field.Choice.Option", SchemaFieldChoiceOption
)


@dataclass(eq=False, repr=False)
class SchemaFieldComputed(betterproto2.Message):
    """

    Computed field kind: a value derived from other values, not entered by
    the user. Every implementation evaluates `expr` and writes the result
    under this field's name, so other expressions and validation rules can
    read it via `root`.

    Evaluation is dependency-ordered: a Computed field may reference inputs
    and other Computed fields. Cycles are rejected at schema-validation
    time. Referencing a value that is absent at evaluation time is an error
    (surfaced as a ValidationError on this field's path).

    `expr` must be a pure, deterministic CEL expression (no now()/rand) so
    all implementations agree.
    """

    expr: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    CEL expression producing the value. Reads `root` (inputs + already
    computed values). Bound the same way as Rule expressions. 
    """

    result: "SchemaFieldResultType | None" = betterproto2.field(
        2, betterproto2.TYPE_ENUM, optional=True
    )
    """
    Result type, used to marshal the value back into the form as a
    canonical Value variant. Optional: if unset the implementation
    infers the type from the type-checked expression. 
    """


default_message_pool.register_message(
    "schemapb", "Schema.Field.Computed", SchemaFieldComputed
)


@dataclass(eq=False, repr=False)
class SchemaFieldDouble(betterproto2.Message):
    """
    Double field kind and its single-field constraints.
    """

    default: "float | None" = betterproto2.field(
        1, betterproto2.TYPE_DOUBLE, optional=True
    )
    """
    Default value used when the field is unset. 
    """

    const: "float | None" = betterproto2.field(
        2, betterproto2.TYPE_DOUBLE, optional=True
    )
    """
    Value must equal exactly this. 
    """

    gt: "float | None" = betterproto2.field(3, betterproto2.TYPE_DOUBLE, optional=True)
    """
    Exclusive minimum: value > gt. 
    """

    gte: "float | None" = betterproto2.field(4, betterproto2.TYPE_DOUBLE, optional=True)
    """
    Inclusive minimum: value >= gte. 
    """

    lt: "float | None" = betterproto2.field(5, betterproto2.TYPE_DOUBLE, optional=True)
    """
    Exclusive maximum: value < lt. 
    """

    lte: "float | None" = betterproto2.field(6, betterproto2.TYPE_DOUBLE, optional=True)
    """
    Inclusive maximum: value <= lte. 
    """

    in_: "list[float]" = betterproto2.field(7, betterproto2.TYPE_DOUBLE, repeated=True)
    """
    Value must be one of these. 
    """

    not_in: "list[float]" = betterproto2.field(
        8, betterproto2.TYPE_DOUBLE, repeated=True
    )
    """
    Value must not be any of these. 
    """

    multiple_of: "float | None" = betterproto2.field(
        9, betterproto2.TYPE_DOUBLE, optional=True
    )
    """
    Divisibility: quotient value/multiple_of must be whole. 
    """


default_message_pool.register_message(
    "schemapb", "Schema.Field.Double", SchemaFieldDouble
)


@dataclass(eq=False, repr=False)
class SchemaFieldDuration(betterproto2.Message):
    """
    Duration field kind with optional range bounds.
    """

    default: "datetime.timedelta | None" = betterproto2.field(
        1,
        betterproto2.TYPE_MESSAGE,
        unwrap=lambda: _google__protobuf__.Duration,
        optional=True,
    )
    """
    Default value used when the field is unset. 
    """

    gt: "datetime.timedelta | None" = betterproto2.field(
        2,
        betterproto2.TYPE_MESSAGE,
        unwrap=lambda: _google__protobuf__.Duration,
        optional=True,
    )
    """
    Exclusive minimum: value > gt. 
    """

    gte: "datetime.timedelta | None" = betterproto2.field(
        3,
        betterproto2.TYPE_MESSAGE,
        unwrap=lambda: _google__protobuf__.Duration,
        optional=True,
    )
    """
    Inclusive minimum: value >= gte. 
    """

    lt: "datetime.timedelta | None" = betterproto2.field(
        4,
        betterproto2.TYPE_MESSAGE,
        unwrap=lambda: _google__protobuf__.Duration,
        optional=True,
    )
    """
    Exclusive maximum: value < lt. 
    """

    lte: "datetime.timedelta | None" = betterproto2.field(
        5,
        betterproto2.TYPE_MESSAGE,
        unwrap=lambda: _google__protobuf__.Duration,
        optional=True,
    )
    """
    Inclusive maximum: value <= lte. 
    """


default_message_pool.register_message(
    "schemapb", "Schema.Field.Duration", SchemaFieldDuration
)


@dataclass(eq=False, repr=False)
class SchemaFieldFloat(betterproto2.Message):
    """

    Float field kind and its single-field constraints.
    gt = exclusive min (> 0), gte = inclusive min (>= 1).
    lt = exclusive max, lte = inclusive max.
    """

    default: "float | None" = betterproto2.field(
        1, betterproto2.TYPE_FLOAT, optional=True
    )
    """
    Default value used when the field is unset. 
    """

    const: "float | None" = betterproto2.field(
        2, betterproto2.TYPE_FLOAT, optional=True
    )
    """
    Value must equal exactly this. 
    """

    gt: "float | None" = betterproto2.field(3, betterproto2.TYPE_FLOAT, optional=True)
    """
    Exclusive minimum: value > gt. 
    """

    gte: "float | None" = betterproto2.field(4, betterproto2.TYPE_FLOAT, optional=True)
    """
    Inclusive minimum: value >= gte. 
    """

    lt: "float | None" = betterproto2.field(5, betterproto2.TYPE_FLOAT, optional=True)
    """
    Exclusive maximum: value < lt. 
    """

    lte: "float | None" = betterproto2.field(6, betterproto2.TYPE_FLOAT, optional=True)
    """
    Inclusive maximum: value <= lte. 
    """

    in_: "list[float]" = betterproto2.field(7, betterproto2.TYPE_FLOAT, repeated=True)
    """
    Value must be one of these. 
    """

    not_in: "list[float]" = betterproto2.field(
        8, betterproto2.TYPE_FLOAT, repeated=True
    )
    """
    Value must not be any of these. 
    """

    multiple_of: "float | None" = betterproto2.field(
        9, betterproto2.TYPE_FLOAT, optional=True
    )
    """
    Divisibility: quotient value/multiple_of must be whole. 
    """


default_message_pool.register_message(
    "schemapb", "Schema.Field.Float", SchemaFieldFloat
)


@dataclass(eq=False, repr=False)
class SchemaFieldInt32(betterproto2.Message):
    """
    Int32 field kind and its single-field constraints.
    """

    default: "int | None" = betterproto2.field(
        1, betterproto2.TYPE_INT32, optional=True
    )
    """
    Default value used when the field is unset. 
    """

    const: "int | None" = betterproto2.field(2, betterproto2.TYPE_INT32, optional=True)
    """
    Value must equal exactly this. 
    """

    gt: "int | None" = betterproto2.field(3, betterproto2.TYPE_INT32, optional=True)
    """
    Exclusive minimum: value > gt. 
    """

    gte: "int | None" = betterproto2.field(4, betterproto2.TYPE_INT32, optional=True)
    """
    Inclusive minimum: value >= gte. 
    """

    lt: "int | None" = betterproto2.field(5, betterproto2.TYPE_INT32, optional=True)
    """
    Exclusive maximum: value < lt. 
    """

    lte: "int | None" = betterproto2.field(6, betterproto2.TYPE_INT32, optional=True)
    """
    Inclusive maximum: value <= lte. 
    """

    in_: "list[int]" = betterproto2.field(7, betterproto2.TYPE_INT32, repeated=True)
    """
    Value must be one of these. 
    """

    not_in: "list[int]" = betterproto2.field(8, betterproto2.TYPE_INT32, repeated=True)
    """
    Value must not be any of these. 
    """

    multiple_of: "int | None" = betterproto2.field(
        9, betterproto2.TYPE_INT32, optional=True
    )
    """
    Divisibility: value % multiple_of must be 0. 
    """


default_message_pool.register_message(
    "schemapb", "Schema.Field.Int32", SchemaFieldInt32
)


@dataclass(eq=False, repr=False)
class SchemaFieldInt64(betterproto2.Message):
    """
    Int64 field kind and its single-field constraints.
    """

    default: "int | None" = betterproto2.field(
        1, betterproto2.TYPE_INT64, optional=True
    )
    """
    Default value used when the field is unset. 
    """

    const: "int | None" = betterproto2.field(2, betterproto2.TYPE_INT64, optional=True)
    """
    Value must equal exactly this. 
    """

    gt: "int | None" = betterproto2.field(3, betterproto2.TYPE_INT64, optional=True)
    """
    Exclusive minimum: value > gt. 
    """

    gte: "int | None" = betterproto2.field(4, betterproto2.TYPE_INT64, optional=True)
    """
    Inclusive minimum: value >= gte. 
    """

    lt: "int | None" = betterproto2.field(5, betterproto2.TYPE_INT64, optional=True)
    """
    Exclusive maximum: value < lt. 
    """

    lte: "int | None" = betterproto2.field(6, betterproto2.TYPE_INT64, optional=True)
    """
    Inclusive maximum: value <= lte. 
    """

    in_: "list[int]" = betterproto2.field(7, betterproto2.TYPE_INT64, repeated=True)
    """
    Value must be one of these. 
    """

    not_in: "list[int]" = betterproto2.field(8, betterproto2.TYPE_INT64, repeated=True)
    """
    Value must not be any of these. 
    """

    multiple_of: "int | None" = betterproto2.field(
        9, betterproto2.TYPE_INT64, optional=True
    )
    """
    Divisibility: value % multiple_of must be 0. 
    """


default_message_pool.register_message(
    "schemapb", "Schema.Field.Int64", SchemaFieldInt64
)


@dataclass(eq=False, repr=False)
class SchemaFieldJson(betterproto2.Message):
    """

    Json field kind: a free-form value accepted WITHOUT structural
    validation — any Value variant (scalar, list, struct, null) passes.
    Use for passthrough configuration blobs, annotations, extension data.
    CEL sees the value as `dyn`; Field.rules still apply if authored.
    """

    default: "Value | None" = betterproto2.field(
        1, betterproto2.TYPE_MESSAGE, optional=True
    )
    """
    Default value used when the field is unset. 
    """


default_message_pool.register_message("schemapb", "Schema.Field.Json", SchemaFieldJson)


@dataclass(eq=False, repr=False)
class SchemaFieldList(betterproto2.Message):
    """
    List field kind: a repeated value described by its element field(s).
    """

    items: "list[SchemaField]" = betterproto2.field(
        1, betterproto2.TYPE_MESSAGE, repeated=True
    )
    """
    Element field definition(s) describing list items. 
    """

    min_items: "int | None" = betterproto2.field(
        2, betterproto2.TYPE_UINT64, optional=True
    )
    """
    Minimum number of items. 
    """

    max_items: "int | None" = betterproto2.field(
        3, betterproto2.TYPE_UINT64, optional=True
    )
    """
    Maximum number of items. 
    """

    unique: "bool" = betterproto2.field(4, betterproto2.TYPE_BOOL)
    """
    If true, items must be unique. 
    """

    count_expr: "str | None" = betterproto2.field(
        5, betterproto2.TYPE_STRING, optional=True
    )
    """
    
    CEL expression over `root` returning a non-negative int: the exact
    number of items the list must have. Renderers generate that many
    item slots. The list length must equal the result, else
    ERROR_CODE_LIST_COUNT_MISMATCH. Empty/absent => length bounded only
    by min_items/max_items. A non-int or negative result is a runtime
    error. Each item is still validated by the item schema; inside an
    item's rules the item's zero-based position is bound as `index`.
    Use case: per-machine settings where N = replicas + 1.
    """


default_message_pool.register_message("schemapb", "Schema.Field.List", SchemaFieldList)


@dataclass(eq=False, repr=False)
class SchemaFieldMap(betterproto2.Message):
    """

    Map field kind: free-form string keys (never rejected — e.g. user-chosen
    subnet/VM names) with values validated against a single shared Schema.
    Unlike Object (fixed, named fields), a Map's key set is arbitrary and
    runtime-supplied; every value is validated against the same
    `value_schema`, so if that schema is `strict` an unknown key INSIDE a
    value is rejected while the map's own keys stay unconstrained. The
    rejected field path names the map key, e.g.
    "subnets.my-subnet-a.evil_key".
    """

    value_schema: "Schema | None" = betterproto2.field(
        1, betterproto2.TYPE_MESSAGE, optional=True
    )
    """
    Schema every map value must satisfy. Absent => values are accepted
    unvalidated (any object). 
    """

    min_entries: "int | None" = betterproto2.field(
        2, betterproto2.TYPE_UINT64, optional=True
    )
    """
    Minimum number of entries. 
    """

    max_entries: "int | None" = betterproto2.field(
        3, betterproto2.TYPE_UINT64, optional=True
    )
    """
    Maximum number of entries. 
    """


default_message_pool.register_message("schemapb", "Schema.Field.Map", SchemaFieldMap)


@dataclass(eq=False, repr=False)
class SchemaFieldObject(betterproto2.Message):
    """
    Object field kind: a nested sub-form described by its own Schema.
    """

    schema: "Schema | None" = betterproto2.field(
        1, betterproto2.TYPE_MESSAGE, optional=True
    )
    """
    Nested object schema. 
    """


default_message_pool.register_message(
    "schemapb", "Schema.Field.Object", SchemaFieldObject
)


@dataclass(eq=False, repr=False)
class SchemaFieldOneOf(betterproto2.Message):
    """

    OneOf field kind: a discriminated union. The value must be an object
    whose discriminator property selects a variant schema.
    """

    discriminator: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    Property name in the value object that selects the variant. 
    """

    variants: "dict[str, Schema]" = betterproto2.field(
        2,
        betterproto2.TYPE_MAP,
        map_meta=betterproto2.map_meta(
            betterproto2.TYPE_STRING, betterproto2.TYPE_MESSAGE
        ),
    )
    """
    Variant schemas keyed by the discriminator value. 
    """


default_message_pool.register_message(
    "schemapb", "Schema.Field.OneOf", SchemaFieldOneOf
)


@dataclass(eq=False, repr=False)
class SchemaFieldRef(betterproto2.Message):
    """

    Ref field kind: the value is an object validated against another
    schema. The target is selected one of two ways:
      - name: a key in the root schema's defs map (local composition;
        enables recursion — a def may Ref back to itself, terminating on
        the finite data).
      - id:   the SchemaIdentity of a separately-registered schema. The
        identity is PRESERVED on the node (renderers can show/link the
        target), and the referenced schema must be made resolvable —
        either present in the root defs under its identity key, or pulled
        in by Link(resolver) before validation. An id-ref to a schema not
        present in defs is ERROR_CODE_UNKNOWN_REF at validate time.

    Oneofs:
        - target:
    """

    name: "str | None" = betterproto2.field(
        1, betterproto2.TYPE_STRING, optional=True, group="target"
    )
    """
    Name of the def in the root schema's defs map. 
    """

    id: "SchemaIdentity | None" = betterproto2.field(
        2, betterproto2.TYPE_MESSAGE, optional=True, group="target"
    )
    """
    Identity of a registered schema (resolved via defs / Link). 
    """


default_message_pool.register_message("schemapb", "Schema.Field.Ref", SchemaFieldRef)


@dataclass(eq=False, repr=False)
class SchemaFieldRule(betterproto2.Message):
    """

    Rule is a CEL validation expression for cross-field / conditional
    checks.
    Context: `this` = this field's value (field-level rules only);
             `root` = whole form as a map, any field reachable.
    expr must evaluate to bool; true means VALID.
    """

    expr: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    CEL boolean expression; true means the value is valid. 
    """

    message: "str" = betterproto2.field(2, betterproto2.TYPE_STRING)
    """
    Message shown to the user when expr is false. 
    """

    id: "str | None" = betterproto2.field(3, betterproto2.TYPE_STRING, optional=True)
    """
    Stable rule id, for mapping errors back to rules. 
    """

    severity: "SchemaFieldSeverity | None" = betterproto2.field(
        4, betterproto2.TYPE_ENUM, optional=True
    )
    """
    Severity; defaults to ERROR. 
    """


default_message_pool.register_message("schemapb", "Schema.Field.Rule", SchemaFieldRule)


@dataclass(eq=False, repr=False)
class SchemaFieldString(betterproto2.Message):
    """
    String field kind and its single-field constraints.
    """

    default: "str | None" = betterproto2.field(
        1, betterproto2.TYPE_STRING, optional=True
    )
    """
    Default value used when the field is unset. 
    """

    const: "str | None" = betterproto2.field(2, betterproto2.TYPE_STRING, optional=True)
    """
    Value must equal exactly this. 
    """

    len: "int | None" = betterproto2.field(3, betterproto2.TYPE_UINT64, optional=True)
    """
    Exact character (Unicode code point) length required. 
    """

    min_len: "int | None" = betterproto2.field(
        4, betterproto2.TYPE_UINT64, optional=True
    )
    """
    Minimum character length. 
    """

    max_len: "int | None" = betterproto2.field(
        5, betterproto2.TYPE_UINT64, optional=True
    )
    """
    Maximum character length. 
    """

    pattern: "str | None" = betterproto2.field(
        6, betterproto2.TYPE_STRING, optional=True
    )
    """
    RE2 regular expression the value must match. 
    """

    in_: "list[str]" = betterproto2.field(7, betterproto2.TYPE_STRING, repeated=True)
    """
    Value must be one of these. 
    """

    not_in: "list[str]" = betterproto2.field(8, betterproto2.TYPE_STRING, repeated=True)
    """
    Value must not be any of these. 
    """

    format: "str | None" = betterproto2.field(
        9, betterproto2.TYPE_STRING, optional=True
    )
    """
    
    Semantic format the value must conform to. An OPEN identifier
    resolved against the spec's format registry, not a closed enum:
    adding a format never changes this contract.

    Core formats (every implementation MUST support): "email", "url",
    "uuid", "ipv4", "ipv6", "ip", "hostname", "date", "time",
    "datetime". Registry extensions use namespaced ids (e.g.
    "k8s.quantity") and each ships with its own spec text and
    conformance fixtures; implementations declare which extensions
    they support. A format the implementation does not know is a
    validation failure (ERROR_CODE_UNSUPPORTED_FORMAT) — never a
    silent pass. Empty => no format check.
    """


default_message_pool.register_message(
    "schemapb", "Schema.Field.String", SchemaFieldString
)


@dataclass(eq=False, repr=False)
class SchemaFieldTimestamp(betterproto2.Message):
    """
    Timestamp field kind with optional range bounds.
    """

    default: "datetime.datetime | None" = betterproto2.field(
        1,
        betterproto2.TYPE_MESSAGE,
        unwrap=lambda: _google__protobuf__.Timestamp,
        optional=True,
    )
    """
    Default value used when the field is unset. 
    """

    gt: "datetime.datetime | None" = betterproto2.field(
        2,
        betterproto2.TYPE_MESSAGE,
        unwrap=lambda: _google__protobuf__.Timestamp,
        optional=True,
    )
    """
    Exclusive minimum: value > gt. 
    """

    gte: "datetime.datetime | None" = betterproto2.field(
        3,
        betterproto2.TYPE_MESSAGE,
        unwrap=lambda: _google__protobuf__.Timestamp,
        optional=True,
    )
    """
    Inclusive minimum: value >= gte. 
    """

    lt: "datetime.datetime | None" = betterproto2.field(
        4,
        betterproto2.TYPE_MESSAGE,
        unwrap=lambda: _google__protobuf__.Timestamp,
        optional=True,
    )
    """
    Exclusive maximum: value < lt. 
    """

    lte: "datetime.datetime | None" = betterproto2.field(
        5,
        betterproto2.TYPE_MESSAGE,
        unwrap=lambda: _google__protobuf__.Timestamp,
        optional=True,
    )
    """
    Inclusive maximum: value <= lte. 
    """


default_message_pool.register_message(
    "schemapb", "Schema.Field.Timestamp", SchemaFieldTimestamp
)


@dataclass(eq=False, repr=False)
class SchemaFieldUInt32(betterproto2.Message):
    """
    UInt32 field kind and its single-field constraints.
    """

    default: "int | None" = betterproto2.field(
        1, betterproto2.TYPE_UINT32, optional=True
    )
    """
    Default value used when the field is unset. 
    """

    const: "int | None" = betterproto2.field(2, betterproto2.TYPE_UINT32, optional=True)
    """
    Value must equal exactly this. 
    """

    gt: "int | None" = betterproto2.field(3, betterproto2.TYPE_UINT32, optional=True)
    """
    Exclusive minimum: value > gt. 
    """

    gte: "int | None" = betterproto2.field(4, betterproto2.TYPE_UINT32, optional=True)
    """
    Inclusive minimum: value >= gte. 
    """

    lt: "int | None" = betterproto2.field(5, betterproto2.TYPE_UINT32, optional=True)
    """
    Exclusive maximum: value < lt. 
    """

    lte: "int | None" = betterproto2.field(6, betterproto2.TYPE_UINT32, optional=True)
    """
    Inclusive maximum: value <= lte. 
    """

    in_: "list[int]" = betterproto2.field(7, betterproto2.TYPE_UINT32, repeated=True)
    """
    Value must be one of these. 
    """

    not_in: "list[int]" = betterproto2.field(8, betterproto2.TYPE_UINT32, repeated=True)
    """
    Value must not be any of these. 
    """

    multiple_of: "int | None" = betterproto2.field(
        9, betterproto2.TYPE_UINT32, optional=True
    )
    """
    Divisibility: value % multiple_of must be 0. 
    """


default_message_pool.register_message(
    "schemapb", "Schema.Field.UInt32", SchemaFieldUInt32
)


@dataclass(eq=False, repr=False)
class SchemaFieldUInt64(betterproto2.Message):
    """
    UInt64 field kind and its single-field constraints.
    """

    default: "int | None" = betterproto2.field(
        1, betterproto2.TYPE_UINT64, optional=True
    )
    """
    Default value used when the field is unset. 
    """

    const: "int | None" = betterproto2.field(2, betterproto2.TYPE_UINT64, optional=True)
    """
    Value must equal exactly this. 
    """

    gt: "int | None" = betterproto2.field(3, betterproto2.TYPE_UINT64, optional=True)
    """
    Exclusive minimum: value > gt. 
    """

    gte: "int | None" = betterproto2.field(4, betterproto2.TYPE_UINT64, optional=True)
    """
    Inclusive minimum: value >= gte. 
    """

    lt: "int | None" = betterproto2.field(5, betterproto2.TYPE_UINT64, optional=True)
    """
    Exclusive maximum: value < lt. 
    """

    lte: "int | None" = betterproto2.field(6, betterproto2.TYPE_UINT64, optional=True)
    """
    Inclusive maximum: value <= lte. 
    """

    in_: "list[int]" = betterproto2.field(7, betterproto2.TYPE_UINT64, repeated=True)
    """
    Value must be one of these. 
    """

    not_in: "list[int]" = betterproto2.field(8, betterproto2.TYPE_UINT64, repeated=True)
    """
    Value must not be any of these. 
    """

    multiple_of: "int | None" = betterproto2.field(
        9, betterproto2.TYPE_UINT64, optional=True
    )
    """
    Divisibility: value % multiple_of must be 0. 
    """


default_message_pool.register_message(
    "schemapb", "Schema.Field.UInt64", SchemaFieldUInt64
)


@dataclass(eq=False, repr=False)
class SchemaIdentity(betterproto2.Message):
    """

    SchemaIdentity is the stable, system-assigned identity of a Schema. A Schema
    is addressed by this identity rather than a free-form name.
    """

    namespace: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    Grouping namespace, e.g. a product or team. Optional. 
    """

    name: "str" = betterproto2.field(2, betterproto2.TYPE_STRING)
    """
    Schema name, unique within its namespace. Required. 
    """

    version: "str" = betterproto2.field(3, betterproto2.TYPE_STRING)
    """
    Schema version, e.g. "v1" or a semver string. Optional. 
    """


default_message_pool.register_message("schemapb", "SchemaIdentity", SchemaIdentity)


@dataclass(eq=False, repr=False)
class SchemaRef(betterproto2.Message):
    """

    SchemaRef selects a schema: either inline, or by identity (resolved from a
    registry).

    Oneofs:
        - source:
    """

    id: "SchemaIdentity | None" = betterproto2.field(
        1, betterproto2.TYPE_MESSAGE, optional=True, group="source"
    )
    """
    Resolve a registered schema by identity. 
    """

    schema: "Schema | None" = betterproto2.field(
        2, betterproto2.TYPE_MESSAGE, optional=True, group="source"
    )
    """
    Use this schema directly, without registering it. 
    """


default_message_pool.register_message("schemapb", "SchemaRef", SchemaRef)


@dataclass(eq=False, repr=False)
class StructValue(betterproto2.Message):
    """

    StructValue is a string-keyed map of values: the representation of a whole
    form, an Object field's value, and a Map field's value.
    """

    fields: "dict[str, Value]" = betterproto2.field(
        1,
        betterproto2.TYPE_MAP,
        map_meta=betterproto2.map_meta(
            betterproto2.TYPE_STRING, betterproto2.TYPE_MESSAGE
        ),
    )


default_message_pool.register_message("schemapb", "StructValue", StructValue)


@dataclass(eq=False, repr=False)
class ValidationError(betterproto2.Message):
    """

    ValidationError is a single validation failure. The contract fields are
    path, code and the typed expected/actual values; ordering of errors in a
    ValidationResult is deterministic (schema field order, then constraint
    order) as defined by the spec, so conformance can compare results across
    implementations byte-for-byte.
    """

    path: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    Path to the failed field, e.g. "max_connections",
    "subnets.my-subnet-a.cidr", "replicas[3].name". Empty for form-level
    failures. 
    """

    code: "ErrorCode" = betterproto2.field(
        2, betterproto2.TYPE_ENUM, default_factory=lambda: ErrorCode(0)
    )
    """
    Stable machine code identifying the kind of failure. 
    """

    expected: "Value | None" = betterproto2.field(
        3, betterproto2.TYPE_MESSAGE, optional=True
    )
    """
    The constraint boundary that was violated (min/max/const/pattern/allowed
    set/...), as a canonical Value. Absent when not applicable. 
    """

    actual: "Value | None" = betterproto2.field(
        4, betterproto2.TYPE_MESSAGE, optional=True
    )
    """
    The offending value, canonical form. Absent when not applicable; MASKED
    (absent) for fields marked secret. 
    """

    constraint: "str" = betterproto2.field(5, betterproto2.TYPE_STRING)
    """
    Name of the structured constraint that failed ("gte", "pattern",
    "min_items", ...). Empty for CEL-rule and schema-level failures. 
    """

    expr: "str" = betterproto2.field(6, betterproto2.TYPE_STRING)
    """
    For CEL failures: the source expression that was violated or errored. 
    """

    rule_id: "str | None" = betterproto2.field(
        7, betterproto2.TYPE_STRING, optional=True
    )
    """
    Id of the authored rule that failed, if it declared one. 
    """

    severity: "SchemaFieldSeverity" = betterproto2.field(
        8, betterproto2.TYPE_ENUM, default_factory=lambda: SchemaFieldSeverity(0)
    )
    """
    Severity; defaults to ERROR. WARNING errors do not block submit. 
    """

    message: "str" = betterproto2.field(9, betterproto2.TYPE_STRING)
    """
    Human-readable message rendered from the spec's shared template set.
    Informative only — NOT part of the conformance contract. 
    """


default_message_pool.register_message("schemapb", "ValidationError", ValidationError)


@dataclass(eq=False, repr=False)
class ValidationResult(betterproto2.Message):
    """

    ValidationResult is the complete outcome of validating a form.
    """

    errors: "list[ValidationError]" = betterproto2.field(
        1, betterproto2.TYPE_MESSAGE, repeated=True
    )
    """
    All failures, in the spec's deterministic order. Empty means valid. 
    """


default_message_pool.register_message("schemapb", "ValidationResult", ValidationResult)


@dataclass(eq=False, repr=False)
class Value(betterproto2.Message):
    """

    Value is the typed value container for schemapb. It replaces
    google.protobuf.Value (a JSON model where every number is a double) so that
    integer kinds keep full 64-bit precision across languages.

    Mapping to CEL types is 1:1 and defined by the spec:
      int32/int64   -> int        uint32/uint64 -> uint
      float/double  -> double     bool          -> bool
      string        -> string     duration      -> google.protobuf.Duration
      timestamp     -> google.protobuf.Timestamp
      list          -> list       struct        -> map
      null          -> null       bytes         -> bytes

    In protoJSON, int64/uint64 serialise as strings (standard protobuf JSON
    mapping), so JavaScript clients keep precision via bigint.

    Canonical form (REQUIRED for conformance): a value stored for a field MUST
    use the variant matching the field's declared kind — e.g. an Int64 field's
    value is always `int64_value`, never `int32_value`, even when it fits.

    Oneofs:
        - kind:
    """

    null_value: "NullValue | None" = betterproto2.field(
        1, betterproto2.TYPE_ENUM, optional=True, group="kind"
    )

    bool_value: "bool | None" = betterproto2.field(
        2, betterproto2.TYPE_BOOL, optional=True, group="kind"
    )

    int32_value: "int | None" = betterproto2.field(
        3, betterproto2.TYPE_INT32, optional=True, group="kind"
    )

    int64_value: "int | None" = betterproto2.field(
        4, betterproto2.TYPE_INT64, optional=True, group="kind"
    )

    uint32_value: "int | None" = betterproto2.field(
        5, betterproto2.TYPE_UINT32, optional=True, group="kind"
    )

    uint64_value: "int | None" = betterproto2.field(
        6, betterproto2.TYPE_UINT64, optional=True, group="kind"
    )

    float_value: "float | None" = betterproto2.field(
        7, betterproto2.TYPE_FLOAT, optional=True, group="kind"
    )

    double_value: "float | None" = betterproto2.field(
        8, betterproto2.TYPE_DOUBLE, optional=True, group="kind"
    )

    string_value: "str | None" = betterproto2.field(
        9, betterproto2.TYPE_STRING, optional=True, group="kind"
    )

    duration_value: "datetime.timedelta | None" = betterproto2.field(
        10,
        betterproto2.TYPE_MESSAGE,
        unwrap=lambda: _google__protobuf__.Duration,
        optional=True,
        group="kind",
    )

    timestamp_value: "datetime.datetime | None" = betterproto2.field(
        11,
        betterproto2.TYPE_MESSAGE,
        unwrap=lambda: _google__protobuf__.Timestamp,
        optional=True,
        group="kind",
    )

    list_value: "ListValue | None" = betterproto2.field(
        12, betterproto2.TYPE_MESSAGE, optional=True, group="kind"
    )

    struct_value: "StructValue | None" = betterproto2.field(
        13, betterproto2.TYPE_MESSAGE, optional=True, group="kind"
    )

    bytes_value: "bytes | None" = betterproto2.field(
        14, betterproto2.TYPE_BYTES, optional=True, group="kind"
    )


default_message_pool.register_message("schemapb", "Value", Value)


from ..google import protobuf as _google__protobuf__
